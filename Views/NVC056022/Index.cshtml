@model IEnumerable<NguyenVanCuong56022.NVC056022>

@{
    ViewData["Title"] = "Index";
}

<h1>Danh sách NVC0506022</h1>

<p>
    <a asp-action="Create">Tạo mới</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NVCName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NVCGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NVCName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NVCGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NVCID">Sửa</a> |
                <a asp-action="Details" asp-route-id="@item.NVCID">Chi tiết</a> |
                <a asp-action="Delete" asp-route-id="@item.NVCID">Xóa</a>
            </td>
        </tr>
}
    </tbody>
</table>
